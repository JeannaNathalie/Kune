---
---

<div class="container">
  <img class="front_picture" src="/images/sol-2.svg" alt="" />
</div>
<div class="button">
  <button class="tickets">TICKETS</button>
</div>
<div class="banner">
  <div class="scrolling-text">
    <span>19. - 23 JULY</span>
    <span>19. - 23 JULY</span>
    <span>19. - 23 JULY</span>
  </div>
</div>

<div class="slideshow-container">
  <p>GALLERY</p>
  <div class="mySlides fade">
    <img class="img1" src="public/images/gallery1.webp" alt="img1" />
  </div>
  <div class="mySlides fade">
    <img class="img5" src="public/images/gallery5.webp" alt="img5" />
  </div>
  <div class="mySlides fade">
    <img class="img2" src="public/images/gallery2.webp" alt="img2" />
  </div>
  <div class="mySlides fade">
    <img class="img3" src="public/images/gallery3.webp" alt="img3" />
  </div>
  <div class="mySlides fade">
    <img class="img4" src="public/images/gallery4.webp" alt="img4" />
  </div>

  <a class="prev">&#10094;</a>
  <a class="next">&#10095;</a>
</div>

<script>
  document.querySelector(".prev").addEventListener("click", () => {
    plusSlides(-1);
  });
  document.querySelector(".next").addEventListener("click", () => {
    plusSlides(1);
  });
  var slideIndex = 1;
  showSlides(slideIndex);

  function plusSlides(n) {
    showSlides((slideIndex += n));
  }

  function showSlides(n) {
    var i;
    var slides = document.getElementsByClassName("mySlides");

    if (n > slides.length) {
      slideIndex = 1;
    }
    if (n < 1) {
      slideIndex = slides.length;
    }
    for (i = 0; i < slides.length; i++) {
      slides[i].style.display = "none";
    }

    slides[slideIndex - 1].style.display = "block";
  }
</script>

<style>
  /* MEDIA QUERY */
  @media (max-width: 600px) {
    button {
      background-color: var(--contrast-color);
      color: #fcf0cb;
      padding: 0.9rem 2.5rem 0.9rem 2.5rem;
      border-radius: 25px;
      border: none;
      font-weight: bold;
      box-shadow: 6px 7px 5px rgb(139, 99, 53);
      margin-top: 1rem;
    }

    .button {
      text-align: center;
    }

    .banner {
      width: 100%;
      height: 50px;
      /* overflow: hidden; */
      margin-top: 1em;
      overflow: hidden;
    }

    .scrolling-text {
      animation: scroll 13s linear infinite;
      width: fit-content;
    }

    .scrolling-text span {
      color: white;
      font-size: 0.9em;
      font-weight: bold;
      margin-right: 1em;
      white-space: nowrap;
    }

    @keyframes scroll {
      0% {
        transform: translateX(-100vw);
      }
      100% {
        transform: translateX(100vw);
      }
    }
    .container {
      display: flex;
      justify-content: center;
      align-items: center;
    }
    .container img {
      max-width: 100%;
    }

    .mySlides {
      display: none;
    }

    .slideshow-container {
      max-width: 80%;
      position: relative;
      margin: auto;
    }
    /* Next & previous buttons */
    .prev,
    .next {
      cursor: pointer;
      position: absolute;
      top: 50%;
      width: auto;
      margin-top: -1rem;
      padding: 0.5rem;
      color: var(--text-color);
      font-size: 2.5rem;
      transition: 0.6s ease;
      border-radius: 3px 3px 3px 3px;
      user-select: none;
    }

    /* Position the "next button" to the right */
    .next {
      right: 0;
      border-radius: 3px 0 0 3px;
    }

    /* On hover, add a black background color with a little bit see-through */
    .prev:hover,
    .next:hover {
      color: var(--primary-color);
    }

    /* Fading animation */
    .fade {
      animation-name: fade;
      animation-duration: 1.5s;
    }

    @keyframes fade {
      from {
        opacity: 0.4;
      }
      to {
        opacity: 1;
      }
    }
    .slideshow-container img {
      display: flex;
      margin: 0%;
      max-width: 100%;
    }
    .slideshow-container {
      max-width: 100%;
    }
    .slideshow-container p {
      color: var(--text-color);
      text-align: center;
    }
  }
  @media (min-width: 600px) {
    button {
      text-align: center;
      background-color: var(--contrast-color);
      color: #fcf0cb;
      padding: 0.9rem 2.5rem 0.9rem 2.5rem;
      border-radius: 25px;
      border: none;
      font-weight: bold;
      box-shadow: 6px 7px 5px rgb(139, 99, 53);
      margin-top: 1rem;
    }
    .button {
      text-align: center;
    }

    .banner {
      width: 100%;
      height: 50px;
      overflow: hidden;
      margin-top: 1em;
    }

    .scrolling-text {
      animation: scroll 13s linear infinite;
      width: fit-content;
    }

    .scrolling-text span {
      color: white;
      font-size: 1.5em;
      font-weight: bold;
      margin-right: 4em;
      white-space: nowrap;
    }

    @keyframes scroll {
      0% {
        transform: translateX(-100vw);
      }
      100% {
        transform: translateX(100vw);
      }
    }

    .slideshow-container p {
      color: var(--text-color);
      text-align: center;
      font-size: 2em;
    }
    .container {
      display: flex;
      justify-content: center;
      align-items: center;
      margin-top: 0%;
      padding-top: 0%;
    }

    .container img {
      margin-top: 4em;
      display: flex;
      align-items: center;
      justify-content: center;
      max-width: 100%;
    }

    .mySlides {
      display: none;
    }

    .slideshow-container {
      max-width: 80%;
      position: relative;
      margin: auto;
    }
    /* Next & previous buttons */
    .prev,
    .next {
      cursor: pointer;
      position: absolute;
      top: 50%;
      width: auto;
      margin-top: -1rem;
      padding: 0.5rem;
      color: var(--text-color);
      font-size: 4rem;
      transition: 0.6s ease;
      border-radius: 3px 3px 3px 3px;
      user-select: none;
    }

    /* Position the "next button" to the right */
    .next {
      right: 0;
      border-radius: 3px 0 0 3px;
    }

    /* On hover, add a black background color with a little bit see-through */
    .prev:hover,
    .next:hover {
      color: var(--primary-color);
    }

    /* Fading animation */
    .fade {
      animation-name: fade;
      animation-duration: 1.5s;
    }

    @keyframes fade {
      from {
        opacity: 0.4;
      }
      to {
        opacity: 1;
      }
    }
    .slideshow-container img {
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
      margin: 0%;
      max-width: 100%;
    }
    .slideshow-container {
      max-width: 80%;
    }
  }
</style>
